const cats=['Cat Tembok','Cat Kendaraan','Thinner'];let products=[
{id:'navilex',name:'Navilex',category:'Cat Tembok',use:'Dinding',desc:'Pilihan warna cat tembok untuk memperbarui tampilan dinding ruang. Jelajahi warna berdasarkan kode pada katalog; ukuran yang tercantum merupakan referensi produk sejenis dan perlu dikonfirmasi.',pack:'1 kg · 5 kg · 20 kg · 25 kg',status:'published',code:'NAVILEX',colors:window.ChemmicalNavilexCatalog.groups},
{id:'cat-kendaraan',name:'Cat Kendaraan',category:'Cat Kendaraan',use:'Kendaraan',desc:'Kategori cat untuk kebutuhan pengecatan kendaraan. Pilihan produk, kemasan, dan petunjuk aplikasi akan dilengkapi setelah informasi resmi tersedia.',pack:'0,5 L · 1 L',status:'published',code:''},
{id:'thinner',name:'Thinner',category:'Thinner',use:'Informasi menyusul',desc:'Rincian jenis produk, penggunaan, dan kemasan akan dilengkapi setelah informasi resmi tersedia.',pack:'0,5 L · 1 L · 5 L',status:'published',code:''}
];let messages=[];let company={name:'CV Mitra Chemical Indonesia',desc:'Cat tembok, cat kendaraan, dan thinner dengan merek sendiri.',email:'Belum diisi',phone:'+62 822-6079-9626',address:'Kawasan Modern Kav. 24, Cikande, Kabupaten Serang, Banten'};const app=document.getElementById('app'),dialog=document.getElementById('dialog');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const slug=c=>c.toLowerCase().replaceAll(' ','-');const art=p=>p.id==='navilex'?`<div class="product-art navilex-art"><strong>NAVILEX</strong><small>CAT TEMBOK · PILIHAN WARNA</small><span class="navilex-art-colors" aria-hidden="true">${p.colors[0].colors.slice(0,6).map(c=>`<i style="background:${c[2]}"></i>`).join('')}</span></div>`:`<div class="product-art"><strong>CI</strong><small>DETAIL VISUAL MENYUSUL</small></div>`;
const card=p=>`<a class="card" href="#detail/${p.id}">${art(p)}<div class="card-body"><span class="eyebrow">${esc(p.category)}</span><h3>${esc(p.name)}</h3><p>${esc(p.use)} · ${p.id==='navilex'?'30 warna berkode':'Rincian produk menyusul'}</p><span class="muted">Lihat produk ↗</span></div></a>`;
const productSearchText=p=>([p.name,p.code,...(p.colors||[]).flatMap(g=>g.colors.flatMap(c=>[c[0],c[1]]))].join(' ')).toLowerCase();
function route(){return (location.hash.slice(1)||'home').split('?')}function params(){return new URLSearchParams(route()[1]||'')}function go(r){location.hash=r}function note(){return '<div class="note">Data dan kemasan pada prototipe ini adalah contoh. Spesifikasi resmi akan diisi kemudian.</div>'}function wrap(s){return '<div class="wrap">'+s+'</div>'}function crumb(s){return `<div class="crumb"><a href="#home">Beranda</a> / ${s}</div>`}function title(s){document.title=s+' — CV Mitra Chemical Indonesia'}function home(){return `<section class="reference-hero wrap"><span class="eyebrow">CV MITRA CHEMICAL INDONESIA</span><h1>Cat dan thinner untuk<br>berbagai kebutuhan Anda.</h1><p>Jelajahi produk untuk dinding, kendaraan, dan kebutuhan pengencer.<br>Temukan informasi produk sebelum menghubungi tim kami.</p><div class="photo-categories">${cats.map((c,i)=>`<a class="photo-category" href="#products?category=${encodeURIComponent(c)}"><img src="${['hero.jpg','automotive.jpg','laboratory.jpg'][i]}" alt="${['Ilustrasi interior dengan dinding berwarna','Ilustrasi permukaan bodi kendaraan','Ilustrasi peralatan laboratorium'][i]}"><span class="photo-category-label"><strong>${c}</strong><span>${['Pilihan produk untuk permukaan dinding.','Pilihan produk pengecatan kendaraan.','Pilihan pengencer untuk kebutuhan aplikasi.'][i]}</span><b aria-hidden="true">↗</b></span></a>`).join('')}</div><p class="image-disclosure">Gambar kategori merupakan ilustrasi konsep.</p></section><section class="navy-company"><div class="wrap"><div class="company-heading"><span class="eyebrow">TENTANG PERUSAHAAN</span><h2>CV Chemmical<br>Indonesia</h2><p>Berlokasi di Cikande, Banten, dengan kategori cat tembok, cat kendaraan, dan thinner bermerek sendiri.</p><a class="btn" href="#about">Tentang Kami</a></div><div class="company-links"><article><span>01 / PRODUK</span><h3>Temukan produk<br>sesuai kebutuhan.</h3><p>Lihat kegunaan, kemasan, dan informasi masing-masing produk.</p><a href="#products">Jelajahi katalog →</a></article><article><span>02 / CONTACT US</span><h3>Diskusikan kebutuhan<br>pengadaan Anda.</h3><p>Sampaikan pertanyaan produk atau kebutuhan kerja sama melalui formulir.</p><a href="#contact">Hubungi kami →</a></article></div></div></section><section class="wrap reference-products"><div class="section-head"><div><span class="eyebrow">KATALOG</span><h2>Informasi produk</h2></div><a href="#products">Lihat semua produk →</a></div><div class="grid">${products.filter(p=>p.status==='published').slice(0,3).map(card).join('')}</div></section><section class="wrap location-section"><div><span class="eyebrow">LOKASI PERUSAHAAN</span><h2>${esc(company.address)}</h2><p>Lihat detail alamat melalui Contact Us.</p></div><a class="btn" href="#contact">Contact Us ↗</a></section>`}function catalog(){const q=params(),cat=q.get('category')||'',search=q.get('q')||'',use=q.get('use')||'';const list=products.filter(p=>p.status==='published'&&(!cat||p.category===cat)&&(!use||p.use===use)&&(!search||productSearchText(p).includes(search.toLowerCase())));const values=[...new Set(products.filter(p=>p.status==='published'&&(!cat||p.category===cat)).map(p=>p.use))];window.filterValues=values;return wrap(crumb('Produk')+`<span class="eyebrow">KATALOG PRODUK</span><h1>${esc(cat||'Temukan produk Anda.')}</h1><p>Pilih kategori, atau cari nama dan kode warna Navilex yang Anda butuhkan.</p><div class="tabs">${['',...cats].map(c=>`<a class="${c===cat?'active':''}" href="#products?category=${encodeURIComponent(c)}&q=${encodeURIComponent(search)}">${c||'Semua Produk'}</a>`).join('')}</div><form class="search" onsubmit="searchCatalog(event)"><input aria-label="Cari produk" name="q" placeholder="Cari nama atau kode produk…" value="${esc(search)}"><button>Cari</button><button type="button" class="secondary mobile-filter" onclick="filterModal()">Filter</button></form><div class="catalog"><aside class="aside"><h3>Filter produk</h3><p style="font-size:13px">Kegunaan</p>${values.map(v=>`<label class="check"><input type="checkbox" ${v===use?'checked':''} value="${esc(v)}" onchange="setFilter(this.checked?this.value:'')">${esc(v)}</label>`).join('')}<button class="secondary" onclick="setFilter('')">Reset Filter</button></aside><div><div class="results-bar"><span>${list.length} produk ditemukan</span><span>Warna digital adalah pratinjau</span></div><div class="grid">${list.length?list.map(card).join(''):'<div class="empty"><h3>Produk belum ditemukan</h3><p>Coba kata kunci lain atau reset filter.</p><a class="btn secondary" href="#products">Reset Pencarian</a></div>'}</div>${list.length?'<div class="pagination">Halaman 1 dari 1</div>':''}</div></div>`)}function searchCatalog(e){e.preventDefault();const p=params();p.set('q',new FormData(e.target).get('q').trim());go('products?'+p)}function setFilter(v){const p=params();v?p.set('use',v):p.delete('use');go('products?'+p)}function filterModal(){const active=params().get('use')||'';dialog.innerHTML=`<h2>Filter produk</h2><label>Kegunaan<select id="filter-select"><option value="">Semua kegunaan</option>${filterValues.map(v=>`<option ${v===active?'selected':''}>${esc(v)}</option>`).join('')}</select></label><div class="row-actions"><button onclick="setFilter(document.getElementById('filter-select').value);dialog.close()">Terapkan Filter</button><button class="secondary" onclick="dialog.close()">Tutup</button></div>`;dialog.showModal()}function navilexPalette(p){return `<section class="navilex-palette" aria-labelledby="navilex-colors-title"><div class="palette-intro"><span class="eyebrow">KATALOG WARNA NAVILEX</span><h2 id="navilex-colors-title">Temukan warna untuk ruang Anda.</h2><p>Empat kelompok pertama memuat 30 warna berkode. Warna ungu, netral, dan abu-abu ditampilkan sebagai pratinjau dari gambar referensi, sementara kodenya menunggu konfirmasi.</p></div>${p.colors.map(g=>`<section class="palette-group"><h3>${esc(g.name)} <small>${g.colors.length} pratinjau</small></h3>${g.previewOnly?'<p class="palette-pending">Kode warna kelompok ini belum tersedia di gambar referensi.</p>':''}<div class="palette-grid">${g.colors.map((c,i)=>`<div class="palette-color"><div class="palette-chip" style="background-color:${c[2]}" role="img" aria-label="Contoh ${esc(g.name)} ${esc(c[1]||`nomor ${i+1}`)}"></div><div class="palette-color-caption">${c[0]?`<strong>${esc(c[0])}</strong>`:''}<span>${esc(c[1]||`Pratinjau ${String(i+1).padStart(2,'0')}`)}</span></div></div>`).join('')}</div></section>`).join('')}<p class="palette-disclaimer">Tampilan warna di layar hanya pratinjau dan dapat berbeda dari hasil cat sebenarnya. Kode dan nama pada empat kelompok pertama mengikuti gambar katalog. Referensi netral dan abu-abu mengulang nama pada gambar ungu, sehingga nama warna netral dan abu-abu tidak ditampilkan sebagai nama resmi.</p></section>`}
function detail(id){const p=products.find(p=>p.id===id);if(!p||p.status!=='published')return wrap('<h1>Produk tidak aktif</h1><a class="btn" href="#products">Kembali ke Produk</a>');title(p.name);if(p.id==='navilex')return wrap(crumb(`<a href="#products">Produk</a> / Navilex`)+`<div class="detail navilex-detail"><div>${art(p)}</div><div><span class="eyebrow">CAT TEMBOK · NAVILEX</span><h1>Navilex</h1><p>${esc(p.desc)}</p><div class="specs"><div><small>Penggunaan</small>Permukaan dinding</div><div><small>Pilihan warna</small>30 kode warna; 36 pratinjau tanpa kode</div><div><small>Contoh ukuran kemasan produk sejenis</small>${esc(p.pack)}</div></div><p><a class="btn" href="#contact?product=${p.id}">Tanyakan Produk ↗</a></p></div></div>${navilexPalette(p)}<div class="split"><section><h2>Informasi produk</h2><p>Navilex ditampilkan sebagai pilihan cat tembok. Jenis permukaan yang sesuai, cara aplikasi, ketersediaan ukuran, dan dokumen teknis akan dilengkapi setelah diverifikasi.</p></section><section><h2>Butuh bantuan memilih?</h2><p>Sertakan nama produk dan kode warna dalam pesan Anda agar tim dapat menindaklanjuti kebutuhan Anda.</p><a href="#contact?product=navilex">Contact Us →</a></section></div>`);return wrap(crumb(`<a href="#products">Produk</a> / ${esc(p.name)}`)+`<div class="detail"><div>${art(p)}</div><div><span class="eyebrow">${esc(p.category)}</span><h1>${esc(p.name)}</h1><p>${esc(p.desc)}</p><h3>Contoh ukuran kemasan produk sejenis</h3><span class="pill">${esc(p.pack)}</span><p class="pack-note">Ketersediaan ukuran produk ini perlu dikonfirmasi.</p><p><a class="btn" href="#contact?product=${p.id}">Contact Us ↗</a></p></div></div><div class="note">Pilihan ukuran ini mengikuti contoh produk sejenis. Ketersediaan ukuran dan rincian teknis produk ini belum dikonfirmasi.</div><div class="split"><section><h2>Tentang produk</h2><div class="specs"><div><small>Kategori</small>${esc(p.category)}</div><div><small>Kegunaan</small>${esc(p.use)}</div><div><small>Spesifikasi teknis</small>Menunggu data resmi</div></div></section><section><h2>Informasi lebih lanjut</h2><p>Hubungi kami melalui formulir untuk menanyakan ketersediaan dan informasi produk.</p><a href="#products">← Kembali ke katalog</a></section></div>`)}
function contact(){const id=params().get('product')||'';return wrap(crumb('Contact Us')+`<span class="eyebrow">HUBUNGI KAMI</span><h1>Mari bicarakan<br>kebutuhan Anda.</h1><div class="split"><aside class="contact-info"><h3>${esc(company.name)}</h3><p>Informasi produk, permintaan penawaran, dan kerja sama distributor.</p><hr><h3>Kontak perusahaan</h3><p>Email: ${esc(company.email)}<br>Telepon: ${esc(company.phone)}</p><h3>Lokasi perusahaan</h3><p class="location-name">${esc(company.address)}</p><p class="address-note">Lihat detail alamat melalui Contact Us.</p><div class="note">Simulasi saja. Gunakan data contoh; pesan hanya berada dalam sesi browser ini dan tidak dikirim melalui email.</div></aside><section id="contact-area"><form id="contact-form" onsubmit="submitContact(event)"><div class="formgrid"><label>Nama<input name="name" required minlength="2" maxlength="100" autocomplete="off"></label><label>Perusahaan (opsional)<input name="company" maxlength="150"></label><label>Email<input name="email" required type="email" maxlength="254"></label><label>Telepon (opsional)<input name="phone" type="tel" maxlength="30"></label><label>Topik<select name="topic" required><option value="">Pilih topik</option>${['Informasi Produk','Permintaan Penawaran','Kerja Sama Distributor','Lainnya'].map(v=>`<option>${v}</option>`).join('')}</select></label><label>Produk (opsional)<select name="product"><option value="">Pertanyaan umum</option>${products.filter(p=>p.status==='published').map(p=>`<option value="${p.id}" ${id===p.id?'selected':''}>${esc(p.name)}</option>`).join('')}</select></label><label class="full">Pesan<textarea name="message" rows="6" required minlength="10" maxlength="5000" placeholder="Ceritakan kebutuhan, perkiraan jumlah, dan kota tujuan jika diketahui."></textarea></label></div><p style="font-size:14px">Baca <a href="#privacy" style="text-decoration:underline">informasi privasi prototipe</a>. Field bertanda opsional boleh dikosongkan.</p><button type="submit">Simulasikan Kirim Pesan ↗</button></form></section></div>`)}function submitContact(e){e.preventDefault();const d=Object.fromEntries(new FormData(e.target));if(d.name.trim().length<2||d.message.trim().length<10){alert('Isi nama dan pesan dengan lengkap.');return}const p=products.find(p=>p.id===d.product);messages.unshift({...d,id:'SIM-'+String(Date.now()).slice(-7),product:p?.name||'Umum',status:'Baru',mail:'Simulasi',notes:[],quality:'Belum Dinilai'});document.getElementById('contact-area').innerHTML='<div class="success" role="status" tabindex="-1"><span class="eyebrow">SIMULASI BERHASIL</span><h2>Pesan contoh diterima.</h2><p>Pesan tersedia di preview admin selama sesi ini. Tidak ada email nyata yang dikirim.</p><a class="btn" href="#admin/messages">Lihat Preview Admin</a><p><a href="#products">Kembali ke Produk</a></p></div>';document.querySelector('.success').focus()}function adminShell(content,active=''){return wrap(`<div class="admin"><aside class="admin-menu"><span class="eyebrow">ADMIN · SIMULASI</span>${[['','Ringkasan'],['products','Produk'],['categories','Kategori & Filter'],['messages','Pesan Masuk'],['company','Informasi Perusahaan']].map(([p,n])=>`<a class="${p===active?'active':''}" href="#admin${p?'/'+p:''}">${n}</a>`).join('')}<a href="#home">Kembali ke Website ↗</a></aside><section><div class="note">Preview admin tanpa login. Data hanya simulasi dalam sesi ini; perubahan hilang saat halaman dimuat ulang.</div>${content}</section></div>`)}function messageTable(){return `<div class="table-scroll"><table><thead><tr><th>Pesan</th><th>Topik</th><th>Status</th><th>Email</th><th></th></tr></thead><tbody>${messages.map(m=>`<tr><td><b>${esc(m.name)}</b><br><small>${m.id}</small></td><td>${esc(m.topic)}</td><td><span class="badge">${m.status}</span></td><td><span class="badge ${m.mail==='Gagal'?'warn':''}">${m.mail}</span></td><td><a href="#admin/message/${m.id}">Buka ↗</a></td></tr>`).join('')}</tbody></table></div>`}function admin(path){const part=path.split('/')[1]||'';if(!part)return adminShell(`<span class="eyebrow">RUANG KERJA</span><h1>Ringkasan</h1><p>Kelola katalog dan tindak lanjuti pesan pelanggan.</p><div class="stats">${[[products.filter(p=>p.status==='published').length,'Produk aktif'],[products.filter(p=>p.status==='draft').length,'Draft'],[messages.filter(m=>m.status==='Baru').length,'Pesan baru'],[messages.filter(m=>m.mail==='Gagal').length,'Email gagal (contoh)']].map(([n,t])=>`<div class="stat"><strong>${n}</strong><small>${t}</small></div>`).join('')}</div><h3>Pesan terbaru</h3>${messageTable()}`);if(part==='products')return adminShell(`<div class="toolbar"><h1>Produk</h1><button onclick="editProduct()">+ Tambah Produk</button></div><div class="table-scroll"><table><thead><tr><th>Produk</th><th>Kategori</th><th>Status</th><th>Aksi</th></tr></thead><tbody>${products.map(p=>`<tr><td><b>${esc(p.name)}</b><br><small>${esc(p.code)}</small></td><td>${esc(p.category)}</td><td><span class="badge">${p.status}</span></td><td><div class="row-actions"><button class="secondary" onclick="editProduct('${p.id}')">Edit</button><button class="secondary" onclick="changeProduct('${p.id}')">${p.status==='published'?'Arsipkan':'Publikasikan'}</button></div></td></tr>`).join('')}</tbody></table></div>`,'products');if(part==='messages')return adminShell('<h1>Pesan Masuk</h1>'+messageTable(),'messages');if(part==='message'){const m=messages.find(m=>m.id===path.split('/')[2]);if(!m)return adminShell('<h1>Pesan tidak ditemukan</h1>','messages');return adminShell(`<a href="#admin/messages">← Semua pesan</a><h1>${esc(m.id)}</h1><h3>${esc(m.name)}</h3><p>${esc(m.company||'Tanpa perusahaan')} · ${esc(m.email)}</p><span class="pill">${esc(m.topic)}</span><span class="pill">${esc(m.product)}</span><div class="note" style="white-space:pre-wrap">${esc(m.message)}</div><div class="formgrid"><label>Status pesan<select onchange="updateMessage('${m.id}','status',this.value)">${['Baru','Diproses','Selesai'].map(v=>`<option ${m.status===v?'selected':''}>${v}</option>`).join('')}</select></label><label>Kualifikasi<select onchange="updateMessage('${m.id}','quality',this.value)">${['Belum Dinilai','Relevan','Tidak Relevan'].map(v=>`<option ${m.quality===v?'selected':''}>${v}</option>`).join('')}</select></label></div><h3>Notifikasi email</h3><p>Status: ${esc(m.mail)} — seluruh pengiriman adalah simulasi.</p>${m.mail==='Gagal'?`<button class="secondary" onclick="updateMessage('${m.id}','mail','Simulasi berhasil')">Simulasikan Coba Ulang Email</button>`:''}<h3>Catatan internal</h3><form onsubmit="addNote(event,'${m.id}')"><label>Hanya terlihat di preview admin<textarea name="note" required rows="3"></textarea></label><button>Simpan Catatan</button></form><h3>Histori</h3>${m.notes.map(n=>`<div class="history">${esc(n)}</div>`).join('')||'<p>Belum ada aktivitas.</p>'}`,'messages')}if(part==='categories')return adminShell(`<h1>Kategori & Filter</h1><p>Tiga kategori peluncuran. Atribut berikut merupakan data contoh.</p>${cats.map(c=>`<section class="aside" style="margin-bottom:16px"><h3>${c}</h3><p>${products.filter(p=>p.category===c).length} produk</p>${[...new Set(products.filter(p=>p.category===c).map(p=>p.use))].map(v=>`<span class="pill">${esc(v)}</span>`).join('')}</section>`).join('')}<div class="note">Editor kategori dan atribut lengkap direncanakan pada implementasi. Preview ini menampilkan struktur pengelolaannya.</div>`,'categories');if(part==='company')return adminShell(`<h1>Informasi Perusahaan</h1><form onsubmit="saveCompany(event)">${[['name','Nama resmi'],['desc','Deskripsi'],['email','Email publik'],['phone','Telepon'],['address','Alamat']].map(([k,n])=>`<label>${n}<input name="${k}" value="${esc(company[k])}" required></label>`).join('')}<button>Simpan Simulasi</button><p id="saved" role="status"></p></form>`,'company');return adminShell('<h1>Halaman tidak ditemukan</h1>')}function editProduct(id){const p=products.find(p=>p.id===id)||{id:'',name:'',category:cats[0],desc:'',use:'',pack:'',code:'',status:'draft'};dialog.innerHTML=`<h2>${id?'Edit':'Tambah'} Produk</h2><p>Perubahan simulasi, tidak disimpan ke server.</p><form onsubmit="saveProduct(event,'${p.id}')"><label>Nama produk<input name="name" value="${esc(p.name)}" required></label><label>Kategori<select name="category">${cats.map(c=>`<option ${c===p.category?'selected':''}>${c}</option>`).join('')}</select></label><label>Deskripsi<textarea name="desc" required>${esc(p.desc)}</textarea></label><label>Kegunaan<input name="use" value="${esc(p.use)}" required></label><label>Kemasan contoh<input name="pack" value="${esc(p.pack)}" required></label><label>Kode<input name="code" value="${esc(p.code)}"></label><div class="row-actions"><button>Simpan ${id?'Perubahan':'Draft'}</button><button type="button" class="secondary" onclick="dialog.close()">Batal</button></div></form>`;dialog.showModal()}function saveProduct(e,id){e.preventDefault();const d=Object.fromEntries(new FormData(e.target));if(id)Object.assign(products.find(p=>p.id===id),d);else products.push({...d,id:String(Date.now()),status:'draft'});dialog.close();render()}function changeProduct(id){const p=products.find(p=>p.id===id);dialog.innerHTML=`<h2>${p.status==='published'?'Arsipkan':'Publikasikan'} produk contoh?</h2><p>Perubahan hanya berlaku pada katalog simulasi dalam sesi ini.</p><div class="row-actions"><button onclick="confirmProduct('${id}')">Lanjutkan</button><button class="secondary" onclick="dialog.close()">Batal</button></div>`;dialog.showModal()}function confirmProduct(id){const p=products.find(p=>p.id===id);p.status=p.status==='published'?'archived':'published';dialog.close();render()}function updateMessage(id,key,value){const m=messages.find(m=>m.id===id);m[key]=value;m.notes.unshift(new Date().toLocaleTimeString('id-ID')+' — '+key+': '+value);render()}function addNote(e,id){e.preventDefault();const m=messages.find(m=>m.id===id);m.notes.unshift(new FormData(e.target).get('note'));render()}function saveCompany(e){e.preventDefault();Object.assign(company,Object.fromEntries(new FormData(e.target)));document.getElementById('saved').textContent='Perubahan simulasi disimpan untuk sesi ini.'}function render(){const [r]=route();document.querySelector('nav').classList.remove('open');title(r==='home'?'Beranda':'Katalog');if(r==='home')app.innerHTML=home();else if(r==='products')app.innerHTML=catalog();else if(r.startsWith('detail/'))app.innerHTML=detail(r.split('/')[1]);else if(r==='contact')app.innerHTML=contact();else if(r.startsWith('admin'))app.innerHTML=wrap('<h1>Area admin</h1><p>Akses pesan pelanggan melalui halaman admin yang dilindungi.</p><a class="btn" href="/admin">Masuk admin ↗</a>');else if(r==='about')app.innerHTML=wrap(crumb('Tentang Kami')+`<span class="eyebrow">TENTANG KAMI</span><h1>${esc(company.name)}</h1><div class="split"><div><h2>Berlokasi di Cikande, Banten.</h2></div><div><p>${esc(company.desc)}</p><p>Alamat: ${esc(company.address)}. Cakupan layanan akan dilengkapi dengan informasi resmi.</p><a class="btn" href="#contact">Contact Us ↗</a></div></div>`);else if(r==='privacy')app.innerHTML=wrap('<h1>Privasi pengunjung</h1><p>Nama, email, nomor telepon jika diisi, produk yang ditanyakan, dan isi pesan Anda digunakan untuk menanggapi pertanyaan produk atau kerja sama. Pesan tersimpan dalam basis data website dan hanya dapat dibaca oleh admin yang diberi akses.</p><p>Jika notifikasi email telah diaktifkan, salinan pesan akan dikirim ke alamat perusahaan yang ditentukan. Jangan masukkan data sensitif yang tidak diperlukan untuk pertanyaan Anda. Untuk meminta penghapusan pesan, hubungi nomor perusahaan yang tercantum pada Contact Us.</p><a class="btn secondary" href="#contact">Kembali ke Contact Us</a>');else app.innerHTML=wrap('<h1>Halaman tidak ditemukan</h1><a class="btn" href="#products">Jelajahi Produk</a>')}window.addEventListener('hashchange',()=>{render();scrollTo(0,0)});render();

if(document.modelContext?.registerTool){try{Promise.resolve(document.modelContext.registerTool({name:'navigate_product_catalog',description:'Open the prototype catalog with a search term. Does not submit inquiries.',inputSchema:{type:'object',properties:{query:{type:'string'}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:false},execute(input){if(!input||typeof input.query!=='string'||input.query.length>200)throw new Error('Invalid query');go('products?q='+encodeURIComponent(input.query));render();return {view:'catalog',query:input.query};}})).catch(()=>{});}catch{}}
